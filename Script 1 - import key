Script 1
#!/bin/bash

# Imports your SSH public key as an EC2 key pair named "bcitkey" in the us-west-2 region so you can SSH into instances with its private key.
aws ec2 import-key-pair --key-name “bcitkey” --region "us-west-2" --public-key-material fileb://home/Ryan/.ssh/bcitkey.pub
